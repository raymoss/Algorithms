def median_sorted_arrays(input_seq1, input_seq2):
    lenseq1 = len(input_seq1)
    lenseq2 = len(input_seq2)
    i = 0
    j = 0
    first_queue = []
    second_queue = []
    while count < (lenseq1 + lenseq2)//2 and :
        if input_seq1[i]